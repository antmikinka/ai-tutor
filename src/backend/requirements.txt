# Core backend requirements: enough to run the API, WebSocket server and the
# SymPy math engine on any machine (no GPU / ML stack needed).
#
#   pip install -r requirements.txt
#
# For local AI models (Qwen3-Omni, MERaLiON, VibeVoice) additionally install
# requirements-ml.txt.

# Web framework
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
websockets>=12.0
python-multipart>=0.0.9

# Configuration
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0

# Math engine and image handling
sympy>=1.12
numpy>=1.26
Pillow>=10.0

# System metrics (optional at runtime, but cheap)
psutil>=5.9

# Used by start_app.py for the health probe
requests>=2.31

# Windows integration
pywin32>=306; sys_platform == 'win32'
