{
  "name": "AI Math Tutor",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "backend",
      "command": "cd src/backend && ../../venv/bin/python -m uvicorn main:app --host 0.0.0.0 --port 8000"
    },
    {
      "name": "frontend",
      "command": "cd src/renderer && BROWSER=none HOST=0.0.0.0 PORT=3000 REACT_APP_BACKEND_URL=http://localhost:8000 npm start"
    }
  ],
  "ports": [3000, 8000]
}
